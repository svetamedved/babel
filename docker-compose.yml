version: '3.5'
services:
  babel:
    build:
      context: .
      dockerfile: ./dockerfile
    command: npm run start
    container_name: babel
    hostname: svetlanaberrios.com
    environment:
      PORT: 3000
    ports:
      - "0.0.0.0:3000:3000"
      - "0.0.0.0:80:3000"
#    volumes:
#      - "./:/usr/src/app"
